# Program: Display the Average of Numbers Entered
# This program asks the user to input numbers and displays the average of those numbers.

# Create an empty list to store numbers
# Ask the user to input numbers until they enter something that's not a number
# Check if the input is a number
# Stop asking for numbers when the input is not a number
# If numbers were entered, calculate the average and display it
# Calculate the average
# Display the average
