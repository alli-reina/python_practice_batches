# Program: Sort and display numbers in ascending order
# This program asks the user to input numbers and displays them in ascending order

# Create an empty list to store numbers
numbers = []

# Input number from user
num = int(input("Enter a number (or any non-number to stop): "))

# Add the number to the list
# Stop the loop if the user inputs a non-number
# Sort and display the numbers
